[project]
name = "nanobanana"
version = "2.1.0"
description = "AI image generation via Gemini API"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=0.4.0",
]

[tool.uv]
dev-dependencies = []
