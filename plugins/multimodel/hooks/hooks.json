{
  "description": "Multi-model orchestration enforcement - enforces claudish --agent usage and /team workflow rules",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Task",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/enforce-team-rules.sh\"",
            "timeout": 10
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/enforce-team-rules.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
